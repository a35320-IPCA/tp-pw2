<!DOCTYPE html>
<html lang="pt">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Unidades Curriculares - IPCA VNF</title>
  <link rel="stylesheet" href="/css/ipca-theme.css">
</head>
<body class="ipca-crud">
<div class="page-shell">
  <header class="page-hero">
    <h1>Unidades Curriculares</h1>
    <p>Gestao de unidades curriculares.</p>
    <span class="role-badge">Tipo de utilizador: Gestor</span>
  </header>
  <nav>
    <a href="/disciplinas">Unidades Curriculares</a>
    <a href="/cursos">Cursos</a>
    <a href="/matriculas">Matriculas</a>
    <a href="/fichas">Fichas de Aluno</a>
    <a href="/planos">Planos de Estudo</a>
    <a href="/notas">Notas e Pautas</a>
    <a href="/disciplinas?action=logout">Terminar sessao</a>
  </nav>

  <% if (message) { %><div class="message <%= type %>"><%= message %></div><% } %>

  <% if (action === 'create' || action === 'edit') { %>
    <h2><%= action === 'edit' ? 'Editar Unidade Curricular' : 'Nova Unidade Curricular' %></h2>
    <div class="form-box">
      <form method="post" action="/disciplinas">
        <input type="hidden" name="action" value="<%= action === 'edit' ? 'update' : 'create' %>">
        <% if (action === 'edit' && editData) { %>
          <input type="hidden" name="IdDisciplina" value="<%= editData._id %>">
        <% } %>
        <label>Unidade Curricular</label><br>
        <input type="text" name="Disciplina" required value="<%= editData ? editData.disciplina : '' %>"><br>
        <label>Sigla</label><br>
        <input type="text" name="Sigla" required value="<%= editData ? editData.sigla : '' %>"><br>
        <button type="submit"><%= action === 'edit' ? 'Atualizar' : 'Criar' %></button>
        <a class="back-link" href="/disciplinas">Voltar a lista</a>
      </form>
    </div>
  <% } else { %>
    <p><a class="primary-link" href="/disciplinas?action=create">+ Nova unidade curricular</a></p>
    <div class="table-wrap">
      <table>
        <tr><th>Unidade Curricular</th><th>Sigla</th><th>Acoes</th></tr>
        <% disciplinas.forEach((row) => { %>
          <tr>
            <td><%= row.disciplina %></td>
            <td><%= row.sigla %></td>
            <td class="actions">
              <a href="/disciplinas?action=edit&id=<%= row._id %>">Editar</a>
              <form class="inline" method="post" action="/disciplinas" onsubmit="return confirm('Remover unidade curricular?');">
                <input type="hidden" name="action" value="delete">
                <input type="hidden" name="IdDisciplina" value="<%= row._id %>">
                <button type="submit">Eliminar</button>
              </form>
            </td>
          </tr>
        <% }) %>
      </table>
    </div>
  <% } %>
</div>
</body>
</html>
